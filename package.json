{
  "name": "calendario-arnie",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "App per tracciare le attivita svolte nelle arnie, con database Neon su Vercel.",
  "scripts": {
    "dev": "vercel dev",
    "migrate": "node --env-file-if-exists=.env --env-file-if-exists=.env.local scripts/migrate.mjs",
    "test": "node scripts/test-db.mjs && node scripts/test-api.mjs",
    "check": "node --check app.js && node --check api/notes.mjs && node --check lib/db.mjs && node --check scripts/migrate.mjs && node --check scripts/test-db.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
